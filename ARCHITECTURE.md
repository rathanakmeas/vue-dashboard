# Architecture Diagram & System Overview

## System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                     CLIENT BROWSER                          │
│                    http://localhost:5173                    │
├─────────────────────────────────────────────────────────────┤
│  Vue 3 Application                                          │
│  ├─ App.vue (Router View)                                  │
│  ├─ Views/                                                 │
│  │  ├─ Dashboard.vue                                       │
│  │  ├─ Settings.vue                                        │
│  │  ├─ Folder/Recent.vue                                   │
│  │  ├─ Folder/Shared.vue                                   │
│  │  └─ Auth/*                                              │
│  ├─ Components/                                            │
│  │  ├─ Navbar.vue                                          │
│  │  ├─ Sidebar.vue                                         │
│  │  └─ Folder.vue                                          │
│  └─ api.js (API Client)                                    │
└──────────────────────────┬──────────────────────────────────┘
                           │
                    fetch() HTTP/HTTPS
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                   NGINX REVERSE PROXY                       │
│                    (In Production)                          │
├─────────────────────────────────────────────────────────────┤
│  Routes:                                                    │
│  / ──────────────> Frontend (Port 5173)                    │
│  /api ────────────> Backend (Port 5000)                    │
└──────────────────────────┬──────────────────────────────────┘
                           │
         ┌─────────────────┼─────────────────┐
         │                 │                 │
         ▼                 ▼                 ▼
    ┌─────────┐   ┌──────────────┐   ┌──────────────┐
    │FRONTEND │   │   BACKEND    │   │   DATABASE   │
    │Container│   │  Container   │   │  Container   │
    └─────────┘   └──────────────┘   └──────────────┘
```

## Docker Compose Services

```
Docker Network: vue-dashboard-network
│
├── 📦 Frontend Container (Port 5173)
│   ├── Image: node:18-alpine
│   ├── App: Vue 3 + Vite
│   ├── Command: serve -s dist -l 5173
│   └── Volumes: dist/ (built app)
│
├── 📦 Backend Container (Port 5000)
│   ├── Image: node:18-alpine
│   ├── App: Express.js
│   ├── Command: node server.js
│   ├── Env: MONGODB_URI, JWT_SECRET, etc.
│   ├── Health Check: /api/health
│   └── Depends on: MongoDB
│
└── 📦 MongoDB Container (Port 27017)
    ├── Image: mongo:7.0
    ├── Username: admin
    ├── Password: password
    ├── Database: vue-dashboard
    ├── Volumes: mongodb-data (persistent)
    └── Health Check: mongosh ping
```

## Application Flow - Authentication

```
User Interface
      │
      ▼
┌─────────────────────┐
│  Login Component    │
│  (src/views/       │
│   auth/Login.vue)  │
└──────────┬──────────┘
           │
    authAPI.login()
           │
           ▼
┌──────────────────────────────────────┐
│  POST /api/auth/login                │
│  { email, password }                 │
└──────────┬───────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  Backend: authController.login()     │
│  1. Find user by email               │
│  2. Compare password (bcrypt)        │
│  3. Generate JWT token               │
│  4. Return token + user info         │
└──────────┬───────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  Response:                           │
│  {                                   │
│    token: "eyJhb...",                │
│    user: { id, email, name }         │
│  }                                   │
└──────────┬───────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  Frontend: Save token to localStorage│
│  localStorage.setItem('token', ...)  │
└──────────┬───────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│  Redirect to Dashboard               │
│  All future requests include:        │
│  Authorization: Bearer {token}       │
└──────────────────────────────────────┘
```

## API Request with Authentication

```
Browser
  │
  ├─ GET /api/folders
  │  Authorization: Bearer eyJhbGci...
  │
  ▼
Backend
  │
  ├─ Receive request
  │
  ├─ Extract token from header
  │
  ├─ Verify token with JWT
  │  ✓ Valid? → Continue
  │  ✗ Invalid? → Return 403
  │
  ├─ Get userId from token
  │
  ├─ Query MongoDB
  │  db.folders.find({ userId: "..." })
  │
  ▼
MongoDB
  │
  ├─ Find all folders for user
  │
  ├─ Return results
  │
  ▼
Backend
  │
  ├─ Format response
  │  {
  │    folders: [
  │      { _id, name, description, ... },
  │      { _id, name, description, ... }
  │    ]
  │  }
  │
  ▼
Browser
  │
  ├─ Receive JSON
  │
  ├─ Update Vue component state
  │
  ▼
User sees list of folders
```

## Database Schema Relationships

```
                    ┌─────────────────┐
                    │   Users         │
                    ├─────────────────┤
                    │ _id             │
                    │ username        │
                    │ email           │
                    │ password        │◄─────┐
                    │ firstName       │      │
                    │ lastName        │      │
                    │ profilePicture  │      │
                    │ createdAt       │      │
                    │ updatedAt       │      │
                    └────────┬────────┘      │
                             │               │
                      (one-to-many)          │
                             │               │
         ┌───────────────────┼───────────────┼───────────────────┐
         │                   │               │                   │
         ▼                   ▼               │                   ▼
    ┌────────────┐    ┌────────────┐       │            ┌─────────────┐
    │ Folders    │    │ Files      │       │            │ Folders     │
    ├────────────┤    ├────────────┤       │            │ (shared)    │
    │ _id        │    │ _id        │       │            ├─────────────┤
    │ name       │    │ name       │       │            │ sharedWith[]│
    │ userId ────┼────┤ userId ────┼───────┘            │ (references │
    │ folderId   │    │ folderId   │                     │  users)     │
    │ isShared   │    │ fileUrl    │                     └─────────────┘
    │ sharedWith │    │ fileSize   │
    │ createdAt  │    │ fileType   │
    └────────────┘    │ isShared   │
                      └────────────┘
```

## Data Flow - Create Folder

```
┌─────────────────────────────────────┐
│ Vue Component                       │
│ FolderCreate.vue                    │
│                                     │
│ user fills form:                    │
│ - Folder name                       │
│ - Description                       │
└────────────┬────────────────────────┘
             │
             │ @submit
             ▼
┌─────────────────────────────────────┐
│ folderAPI.createFolder()            │
│ in src/api.js                       │
└────────────┬────────────────────────┘
             │
             │ fetch() POST
             ▼
┌─────────────────────────────────────┐
│ Backend: POST /api/folders          │
│ {                                   │
│   name: "My Documents",             │
│   description: "Important files"    │
│ }                                   │
│ Headers:                            │
│   Authorization: Bearer {token}     │
└────────────┬────────────────────────┘
             │
             │ middleware/auth.js
             │ Verify token ✓
             │
             ▼
┌─────────────────────────────────────┐
│ folderController.createFolder()     │
│                                     │
│ 1. Validate input                   │
│ 2. Create folder object:            │
│    {                                │
│      name, description,             │
│      userId: req.userId             │
│    }                                │
└────────────┬────────────────────────┘
             │
             │ mongoose.save()
             ▼
┌─────────────────────────────────────┐
│ MongoDB: folders collection         │
│                                     │
│ Insert document:                    │
│ {                                   │
│   _id: ObjectId(...),               │
│   name: "My Documents",             │
│   description: "Important files",   │
│   userId: ObjectId(...),            │
│   isShared: false,                  │
│   sharedWith: [],                   │
│   createdAt: 2024-01-09...,         │
│   updatedAt: 2024-01-09...          │
│ }                                   │
└────────────┬────────────────────────┘
             │
             │ return saved document
             ▼
┌─────────────────────────────────────┐
│ Backend Response:                   │
│ {                                   │
│   message: "Folder created",        │
│   folder: { ... }                   │
│ }                                   │
└────────────┬────────────────────────┘
             │
             │ response.json()
             ▼
┌─────────────────────────────────────┐
│ Frontend: Handle response           │
│ - Update component state            │
│ - Add folder to list                │
│ - Show success message              │
│ - Redirect to folder view           │
└─────────────────────────────────────┘
```

## Token Flow

```
User Agent                 Application              Database
     │                          │                      │
     │ 1. User enters           │                      │
     │    credentials           │                      │
     │                          │                      │
     ├─────────────────────────>│                      │
     │   email, password        │                      │
     │                          │                      │
     │                          ├─────────────────────>│
     │                          │ Find user by email   │
     │                          │                      │
     │                          │<─────────────────────┤
     │                          │ User document        │
     │                          │ (with hashed pwd)    │
     │                          │                      │
     │                          │ 2. Compare passwords │
     │                          │    (bcrypt.compare)  │
     │                          │                      │
     │<─────────────────────────┤                      │
     │   {                      │                      │
     │    token: "...",         │ 3. JWT generated:    │
     │    user: { ... }         │    payload: {        │
     │   }                      │      userId: "...",  │
     │                          │      iat, exp        │
     │                          │    }                 │
     │                          │    signed with       │
     │                          │    JWT_SECRET        │
     │                          │                      │
     │ 4. Store token in        │                      │
     │    localStorage           │                      │
     │                          │                      │
     │ 5. Use in requests       │                      │
     │                          │                      │
     ├─────────────────────────>│                      │
     │ Authorization:           │                      │
     │ Bearer {token}           │                      │
     │                          │ 6. Verify token      │
     │                          │    - Check signature │
     │                          │    - Check expiry    │
     │                          │    - Extract userId  │
     │                          │                      │
     │                          ├─────────────────────>│
     │                          │ Query with userId    │
     │                          │                      │
     │                          │<─────────────────────┤
     │                          │ Return data          │
     │                          │                      │
     │<─────────────────────────┤                      │
     │   Response with data     │                      │
     │                          │                      │
```

## Environment Configuration

```
Development                    Docker Production
──────────────────────────────────────────────────

.env                          backend/.env
├─ MONGODB_URI                ├─ MONGODB_URI
│  localhost:27017            │  mongodb://admin:pwd@mongodb:27017
│                             │
├─ JWT_SECRET                 ├─ JWT_SECRET
│  dev_key                    │  production_secret_key
│                             │
├─ NODE_ENV                   ├─ NODE_ENV
│  development                │  production
│                             │
├─ PORT                       ├─ PORT
│  5000                       │  5000
│                             │
└─ CORS_ORIGIN                └─ CORS_ORIGIN
   http://localhost:5173         https://yourdomain.com
```

## Deployment Architecture

```
┌──────────────────────────────────────────────────────┐
│              Production Server                       │
├──────────────────────────────────────────────────────┤
│                                                      │
│  Port 80/443                                         │
│  ┌─────────────────────────────────┐                │
│  │  Nginx Reverse Proxy            │                │
│  │  - SSL/TLS Termination          │                │
│  │  - Load Balancing               │                │
│  │  - Caching                      │                │
│  └──────┬──────────────┬───────────┘                │
│         │              │                            │
│         ▼              ▼                            │
│  ┌──────────────────────────────┐                  │
│  │ Docker Container Network     │                  │
│  │                              │                  │
│  │ ┌──────────┐  ┌──────────┐  │                  │
│  │ │Frontend  │  │Backend   │  │                  │
│  │ │Port 5173 │  │Port 5000 │  │                  │
│  │ └──────────┘  └──────┬───┘  │                  │
│  │        ▲             │      │                  │
│  │        └─────┬───────┘      │                  │
│  │              │              │                  │
│  │         ┌────▼─────┐        │                  │
│  │         │ MongoDB  │        │                  │
│  │         │Port 27017│        │                  │
│  │         └──────────┘        │                  │
│  │              ▲              │                  │
│  │              │              │                  │
│  │         Persistent Volume   │                  │
│  │         (Database Data)     │                  │
│  └──────────────────────────────┘                  │
│                                                    │
└──────────────────────────────────────────────────────┘
```

---

This complete system provides:
- ✅ Full-stack architecture
- ✅ Authentication & security
- ✅ Database persistence
- ✅ Scalable containerization
- ✅ Production-ready deployment
